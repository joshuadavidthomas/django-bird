[project]
name = "bird-compiler"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Josh", email = "josh@joshthomas.dev" }
]
requires-python = ">=3.10"
dependencies = []

[tool.maturin]
module-name = "bird_compiler"
python-packages = ["bird_compiler"]
python-source = "src"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"
